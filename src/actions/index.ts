import {
    fetchCoursesRequest,
    fetchCoursesSuccess,
    fetchCoursesError
} from 'actions/course';

export const actions = {
    fetchCoursesRequest,
    fetchCoursesSuccess,
    fetchCoursesError
};
